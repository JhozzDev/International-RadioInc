export interface Pais {
  nombre: string;
  codigo: string;
  ingles: string;
}

export interface Radio {
  id: string;     
  name: string;
  country: string;
  language: string;
  url: string;
  icon: string;
}
